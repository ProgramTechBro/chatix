import '../../domain/entities/share_contact_entity.dart';

class ShareContactModel extends ShareContactEntity {
  const ShareContactModel({
    required super.id,
    required super.name,
    required super.avatarUrl,
  });
}
